{
    "name": "libas-talent-spark",
    "private": "true",
    "workspaces": [
        "apps/*",
        "packages/*"
    ],
    "scripts": {
        "dev": "docker-compose up",
        "test": "echo \"Run tests from apps\"",
        "lint": "echo \"Lint later\""
    }
}